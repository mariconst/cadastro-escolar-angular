import { ValidaCpfDirective } from './valida-cpf.directive';

describe('ValidaCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaCpfDirective();
    expect(directive).toBeTruthy();
  });
});
